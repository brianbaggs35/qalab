# QALab Deployment Configuration
# Copy this file to .env and update the values for your deployment

# === REQUIRED DEPLOYMENT SETTINGS ===
# Your EC2 instance hostname or IP address
DEPLOY_SERVER=your-server.example.com

# SSH configuration for deployment
DEPLOY_USER=deploy
DEPLOY_SSH_KEY=~/.ssh/id_rsa
DEPLOY_SSH_PORT=22

# === DATABASE CONFIGURATION ===
# Required: Database password for the application
QALAB_DATABASE_PASSWORD=your-secure-database-password

# Optional: Use remote database (AWS RDS)
# Leave empty to use local PostgreSQL installation
# DATABASE_HOST=your-rds-instance.amazonaws.com
# DATABASE_USERNAME=qalab
# DATABASE_NAME=qalab_production
# DATABASE_PORT=5432

# === SSL CERTIFICATE CONFIGURATION ===
# Set to 'true' to use Let's Encrypt instead of self-signed certificates
USE_LETSENCRYPT=true
LETSENCRYPT_EMAIL=admin@your-server.example.com

# === EMAIL CONFIGURATION (Required for user registration) ===
# SMTP settings for sending emails
SMTP_ADDRESS=smtp.gmail.com
SMTP_PORT=587
SMTP_DOMAIN=your-server.example.com
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_AUTHENTICATION=plain
SMTP_ENABLE_STARTTLS_AUTO=true

# Application mailer host (should match your domain)
MAILER_HOST=your-server.example.com

# === APPLICATION SETTINGS ===
# Rails secret key base (generate with: rails secret)
SECRET_KEY_BASE=your-secret-key-base

# Rails master key (for encrypted credentials)
RAILS_MASTER_KEY=your-master-key

# === OPTIONAL PERFORMANCE SETTINGS ===
# Puma web server configuration
RAILS_MAX_THREADS=5
PUMA_WORKERS=2
PUMA_THREADS=2,16

# === OPTIONAL MONITORING ===
# Application monitoring service keys
# SENTRY_DSN=your-sentry-dsn
# NEW_RELIC_LICENSE_KEY=your-newrelic-key

# === DEPLOYMENT OPTIONS ===
# Git branch to deploy (default: main)
# DEPLOY_BRANCH=main

# Number of releases to keep
# KEEP_RELEASES=5