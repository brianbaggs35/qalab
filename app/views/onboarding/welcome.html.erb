<% content_for :title, "Welcome to QA Lab" %>

<div class="min-h-screen flex items-center justify-center bg-gradient-to-br from-primary/10 via-base-200 to-secondary/10">
  <div class="card w-full max-w-2xl shadow-2xl bg-base-100">
    <div class="card-body">
      <!-- Welcome Header -->
      <div class="text-center mb-8">
        <div class="text-6xl mb-4">🎉</div>
        <h1 class="text-4xl font-bold text-primary">Welcome to QA Lab!</h1>
        <p class="text-base-content/60 mt-2 text-lg">Let's get you set up with your testing environment</p>
      </div>

      <!-- User Info -->
      <div class="bg-primary/10 rounded-lg p-6 mb-8">
        <div class="flex items-center gap-4">
          <div class="avatar placeholder">
            <div class="bg-primary text-primary-content rounded-full w-16">
              <span class="text-2xl font-bold">
                <%= @user.first_name[0]&.upcase %><%= @user.last_name[0]&.upcase %>
              </span>
            </div>
          </div>
          <div>
            <h2 class="text-2xl font-semibold"><%= @user.full_name %></h2>
            <p class="text-base-content/60"><%= @user.email %></p>
          </div>
        </div>
      </div>

      <!-- Features Overview -->
      <div class="mb-8">
        <h3 class="text-xl font-semibold mb-4">What you can do with QA Lab:</h3>
        
        <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
          <div class="flex items-start gap-3 p-4 bg-base-200 rounded-lg">
            <div class="text-2xl">🔧</div>
            <div>
              <h4 class="font-semibold">Automated Testing</h4>
              <p class="text-sm text-base-content/70">Upload and track results from your test suites</p>
            </div>
          </div>
          
          <div class="flex items-start gap-3 p-4 bg-base-200 rounded-lg">
            <div class="text-2xl">✅</div>
            <div>
              <h4 class="font-semibold">Manual Testing</h4>
              <p class="text-sm text-base-content/70">Create and manage manual test cases</p>
            </div>
          </div>
          
          <div class="flex items-start gap-3 p-4 bg-base-200 rounded-lg">
            <div class="text-2xl">📊</div>
            <div>
              <h4 class="font-semibold">Analytics</h4>
              <p class="text-sm text-base-content/70">Visualize your testing progress and trends</p>
            </div>
          </div>
          
          <div class="flex items-start gap-3 p-4 bg-base-200 rounded-lg">
            <div class="text-2xl">👥</div>
            <div>
              <h4 class="font-semibold">Team Collaboration</h4>
              <p class="text-sm text-base-content/70">Invite team members and manage permissions</p>
            </div>
          </div>
        </div>
      </div>

      <!-- Next Step -->
      <div class="text-center">
        <p class="mb-6 text-base-content/80">Ready to get started? Let's create your organization!</p>
        
        <%= link_to onboarding_organization_path, class: "btn btn-primary btn-lg px-8" do %>
          <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" class="w-5 h-5 mr-2">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
          </svg>
          Create Your Organization
        <% end %>
      </div>

      <!-- Progress Indicator -->
      <div class="mt-8 flex justify-center">
        <div class="flex items-center gap-2">
          <div class="w-3 h-3 bg-primary rounded-full"></div>
          <div class="w-8 h-1 bg-primary/30"></div>
          <div class="w-3 h-3 bg-primary/30 rounded-full"></div>
          <div class="w-8 h-1 bg-primary/30"></div>
          <div class="w-3 h-3 bg-primary/30 rounded-full"></div>
        </div>
      </div>
      
      <div class="text-center mt-2">
        <span class="text-sm text-base-content/60">Step 1 of 3</span>
      </div>
    </div>
  </div>
</div>