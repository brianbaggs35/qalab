<!DOCTYPE html>
<html data-theme="qalab">
  <head>
    <title><%= content_for(:title) || "QA Lab" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <% unless Rails.env.test? %>
      <link href="https://cdn.jsdelivr.net/npm/daisyui@4.12.14/dist/full.css" rel="stylesheet" type="text/css" />
      <script src="https://cdn.tailwindcss.com"></script>
    <% end %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="min-h-screen bg-base-200">
    <!-- Navigation -->
    <div class="navbar bg-base-100 shadow-lg">
      <div class="flex-1">
        <%= link_to root_path, class: "btn btn-ghost text-xl font-bold text-primary" do %>
          QA Lab
        <% end %>
      </div>
      <div class="flex-none">
        <% if user_signed_in? %>
          <div class="dropdown dropdown-end">
            <div tabindex="0" role="button" class="btn btn-ghost btn-circle avatar placeholder">
              <div class="bg-neutral text-neutral-content rounded-full w-10">
                <span class="text-xs font-bold"><%= current_user.first_name&.first %><%= current_user.last_name&.first %></span>
              </div>
            </div>
            <ul tabindex="0" class="menu menu-sm dropdown-content mt-3 z-[1] p-2 shadow bg-base-100 rounded-box w-52">
              <li class="menu-title">
                <span><%= current_user.full_name %></span>
              </li>
              <li><%= link_to "Profile", edit_user_registration_path %></li>
              <% if current_organization %>
                <li><%= link_to "Organization", "#" %></li>
              <% end %>
              <li><%= link_to "Sign out", destroy_user_session_path, data: { "turbo-method": "delete" } %></li>
            </ul>
          </div>
        <% else %>
          <div class="flex gap-2">
            <%= link_to "Sign in", new_user_session_path, class: "btn btn-ghost" %>
            <%= link_to "Sign up", new_user_registration_path, class: "btn btn-primary" %>
          </div>
        <% end %>
      </div>
    </div>

    <!-- Main Navigation -->
    <%= render 'shared/main_navigation' %>

    <!-- Flash messages -->
    <% if notice %>
      <div class="alert alert-success shadow-lg mx-4 mt-4">
        <div>
          <svg xmlns="http://www.w3.org/2000/svg" class="stroke-current flex-shrink-0 h-6 w-6" fill="none" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" /></svg>
          <span><%= notice %></span>
        </div>
      </div>
    <% end %>

    <% if alert %>
      <div class="alert alert-error shadow-lg mx-4 mt-4">
        <div>
          <svg xmlns="http://www.w3.org/2000/svg" class="stroke-current flex-shrink-0 h-6 w-6" fill="none" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 14l2-2m0 0l2-2m-2 2l-2-2m2 2l2 2m7-2a9 9 0 11-18 0 9 9 0 0118 0z" /></svg>
          <span><%= alert %></span>
        </div>
      </div>
    <% end %>

    <!-- Main content -->
    <main class="container mx-auto px-4 py-6">
      <%= yield %>
    </main>

    <!-- Footer -->
    <footer class="footer footer-center p-4 bg-base-100 text-base-content mt-auto">
      <div>
        <p>© <%= Date.current.year %> QA Lab. Built with Rails <%= Rails.version %></p>
      </div>
    </footer>
  </body>
</html>
