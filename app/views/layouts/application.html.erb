<!DOCTYPE html>
<html data-theme="qalab">
  <head>
    <title><%= content_for(:title) || "QA Lab" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <% unless Rails.env.test? %>
      <link href="https://cdn.jsdelivr.net/npm/daisyui@4.12.14/dist/full.css" rel="stylesheet" type="text/css" />
      <script src="https://cdn.tailwindcss.com"></script>
    <% end %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="min-h-screen bg-base-200">
    <!-- System Admin Layout -->
    <% if user_signed_in? && current_user.system_admin? %>
      <%= render 'shared/system_admin_sidebar' %>
    <% else %>
      <!-- Regular User Layout -->
      <!-- Navigation -->
      <div class="navbar bg-base-100 shadow-lg">
        <div class="flex-1">
          <%= link_to root_path, class: "btn btn-ghost text-xl font-bold text-primary" do %>
            QA Lab
          <% end %>
        </div>
        <div class="flex-none">
          <% if user_signed_in? %>
            <div class="dropdown dropdown-end">
              <div tabindex="0" role="button" class="btn btn-ghost btn-circle avatar placeholder">
                <div class="bg-neutral text-neutral-content rounded-full w-10">
                  <span class="text-xs font-bold"><%= current_user.first_name&.first %><%= current_user.last_name&.first %></span>
                </div>
              </div>
              <ul tabindex="0" class="menu menu-sm dropdown-content mt-3 z-[1] p-2 shadow bg-base-100 rounded-box w-52">
                <li class="menu-title">
                  <span><%= current_user.full_name %></span>
                </li>
                <li><%= link_to "Profile", edit_user_registration_path %></li>
                <% if current_organization %>
                  <li><%= link_to "Organization", "#" %></li>
                <% end %>
                <li>
                  <%= form_with url: destroy_user_session_path, method: :delete, local: true, class: "inline" do |f| %>
                    <%= f.submit "Sign out", class: "btn btn-ghost btn-sm p-0 m-0 bg-transparent border-none hover:bg-base-200 text-left w-full" %>
                  <% end %>
                </li>
              </ul>
            </div>
          <% else %>
            <div class="flex gap-2">
              <%= link_to "Sign in", new_user_session_path, class: "btn btn-ghost" %>
              <%= link_to "Sign up", new_user_registration_path, class: "btn btn-primary" %>
            </div>
          <% end %>
        </div>
      </div>

      <!-- Main Navigation -->
      <%= render 'shared/main_navigation' %>

      <!-- Flash messages -->
      <% if notice %>
        <div class="fixed top-20 right-4 z-50 w-full max-w-md">
          <div class="bg-gradient-to-r from-emerald-500 to-green-600 text-white rounded-lg shadow-xl border border-emerald-200/20 backdrop-blur-sm" data-controller="alert" data-alert-auto-close-value="true">
            <div class="flex items-start p-4 gap-3">
              <div class="flex-shrink-0 mt-0.5">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
                </svg>
              </div>
              <div class="flex-1 min-w-0">
                <p class="text-sm font-medium leading-relaxed"><%= notice %></p>
              </div>
              <button class="flex-shrink-0 ml-2 p-1 rounded-full hover:bg-white/20 transition-colors duration-200" data-action="click->alert#close" title="Close">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
                </svg>
              </button>
            </div>
          </div>
        </div>
      <% end %>

      <% if alert %>
        <div class="fixed top-20 right-4 z-50 w-full max-w-md">
          <div class="bg-gradient-to-r from-red-500 to-rose-600 text-white rounded-lg shadow-xl border border-red-200/20 backdrop-blur-sm" data-controller="alert" data-alert-auto-close-value="true">
            <div class="flex items-start p-4 gap-3">
              <div class="flex-shrink-0 mt-0.5">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
                </svg>
              </div>
              <div class="flex-1 min-w-0">
                <p class="text-sm font-medium leading-relaxed"><%= alert %></p>
              </div>
              <button class="flex-shrink-0 ml-2 p-1 rounded-full hover:bg-white/20 transition-colors duration-200" data-action="click->alert#close" title="Close">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
                </svg>
              </button>
            </div>
          </div>
        </div>
      <% end %>

      <!-- Main content -->
      <main class="container mx-auto px-4 py-6">
        <%= yield %>
      </main>

      <!-- Footer -->
      <footer class="footer footer-center p-4 bg-base-100 text-base-content mt-auto">
        <div>
          <p>© <%= Date.current.year %> QA Lab. Built with Rails <%= Rails.version %></p>
        </div>
      </footer>
    <% end %>
  </body>
</html>
